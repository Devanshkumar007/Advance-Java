spring.application.name=Service-Initializer


server.port=8765

#server should not register itself
eureka.client.register-with-eureka=false

#server should not fetch the registry
eureka.client.fetch-registry=false
